---
apiVersion: v1
kind: Service
metadata:
  name: console-web-server
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 8888
      protocol: TCP
  selector:
    app: ssh-client-pod
    group: robot-common-group
    robotType: simulated
